[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "golukituki-geodata"
version = "0.1.0"
description = "Offline geospatial data pipelines for GEOLUKITUKI"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/golukituki_geodata"]
