# M3 journal — leaderboard and abuse controls

Date: 2026-09-19  
Status: implementation complete; production activation needs owner credentials

This journal records both the implementation and the reasoning behind M3. It is
written so the same design can later be reproduced in another small
Cloudflare-based service.

## Part 1 — durable leaderboard

### Data model

Migration `0004_m3_leaderboard.sql` adds one table:

```sql
leaderboard_entries (
  mode,
  normalized_nickname,
  nickname,
  score,
  game_id,
  completed_at
)
```

The composite primary key `(mode, normalized_nickname)` means one player name
can occupy at most one place in each game mode. A player may therefore have one
satellite result and one metro result, but cannot fill all ten positions by
replaying the same mode. A later result replaces the stored entry only when its
score is higher. For an equal score the earlier completion remains.

The query index follows the exact top-ten order:

1. mode;
2. score descending;
3. completion time ascending;
4. normalized nickname ascending as a deterministic final tie-breaker.

Keeping the query and index order aligned lets D1 read the first ten rows
without sorting the complete table.

### Atomic final round

Accepting a guess now uses `D1Database.batch()` for all related writes:

1. conditionally lock the current `game_rounds` row with
   `WHERE guessed_at IS NULL`;
2. advance or complete `games`, but only when that exact round was written by
   this attempt;
3. on round five, copy the server-owned total into `leaderboard_entries` with an
   upsert.

D1 executes a batch transactionally and in order. If one statement fails, the
whole batch rolls back. After the batch, both conditional updates must report
exactly one changed row. This retains the M1 first-guess lock and prevents a
replayed final request from incrementing the total or creating another ranking
entry.

The browser never submits a round score or game total. The leaderboard insert
selects `total_score` from the completed server-side `games` row.

### Expired-game cleanup

Every newly created game performs one bounded maintenance statement before its
own insert. It removes at most 25 active games whose 24-hour expiry has passed.
`ON DELETE CASCADE` removes their unfinished round rows. Bounded opportunistic
cleanup avoids a separate scheduler for the MVP and prevents a single request
from doing unbounded work.

Completed games are not removed by this cleanup because their result is an
audit source for the ranking. A retention/export policy remains release work in
M4.

### API and browser

`GET /api/leaderboard?mode=satellite|metro` returns:

```json
{
  "mode": "metro",
  "entries": [{ "rank": 1, "nickname": "민수", "score": 24000 }]
}
```

The Worker assigns rank numbers after the indexed query. React renders nickname
values as normal text nodes; markup contained in a nickname is never interpreted
as HTML. The start screen reloads the ranking when the selected mode changes,
and the summary screen shows the ranking for the completed game's mode.

### Verification

- migration applied to the existing local D1 database: 4 commands successful;
- Worker unit/API suite: 8 tests passed;
- web component/application suite: 9 tests passed;
- a real five-round metro game completed through HTTP against local Wrangler;
- its D1-backed ranking returned `Ranking QA`, rank 1, score 4,634;
- style and UI tests still keep satellite/metro map behavior separate.

## Part 2 — Turnstile and request protection

The browser explicitly renders Cloudflare Turnstile. Local development and unit
tests use Cloudflare's official always-pass sitekey and secret. A successful
widget callback enables the start button and the token is included only in the
create-game request. On failure the widget is recreated because Turnstile tokens
expire after five minutes and may be verified only once.

The Worker validates every token with the Siteverify endpoint before touching
game storage. The secret exists only in the Worker environment. Production
fails closed when the secret/sitekey owner setup is missing. Siteverify network
failure returns a user-readable `503`; rejected or expired tokens return `403`.

Game creation is protected first by a Rate Limiting binding. Three namespace
IDs keep local, preview, and production counters independent. The current limit
is 20 starts per IP per 60 seconds. This is deliberately permissive because a
school, household, or mobile carrier can place many legitimate players behind
one shared IP. Cloudflare documents this binding as an eventually consistent,
location-local abuse control rather than an exact global counter.

Request JSON is measured after reading as UTF-8, so the 2 KiB cap still works
when `Content-Length` is absent. The Turnstile token itself is capped at its
documented 2,048 characters by the shared Zod contract.

## Part 3 — privacy-safe operations

API middleware emits only method, HTTP status, duration, environment, and a
constant event name. It does not log route URLs, game IDs, nicknames, guesses,
answers, bodies, IP addresses, or Turnstile tokens. Expected D1/quota failures
become a generic `503 SERVICE_UNAVAILABLE`; internal details remain out of the
response and structured log.

Deployment documentation covers production/preview widget setup, secret
commands, hostname behavior, rate-limit trade-offs, D1/Worker dashboards, and
the user-visible behavior for `403`, `429`, and `503`.

## Verification after parts 2–3

- shared core tests: 9 passed;
- Worker tests: 11 passed, including verification rejection, rate limiting, and
  a body larger than 2 KiB without relying on its header;
- web tests: 10 passed, including token callback and form hand-off;
- production Worker dry-run recognizes separate D1 and rate-limit bindings;
- a dummy token was accepted by the real Siteverify endpoint through local
  Wrangler, creating an active satellite game;
- `.dev.vars` was loaded as an ignored secret file and displayed as hidden by
  Wrangler.

## Remaining M3 work

No code work remains in M3. The complete workspace validation and all five
geodata pipeline tests pass. The owner must still create preview/production
Turnstile widgets, save their secrets, set their public sitekeys, replace D1
placeholder IDs, apply remote migrations, and perform the documented preview
smoke test. Those actions require the owner's Cloudflare account and are not
safe to simulate in source code.
